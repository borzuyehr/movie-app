#set( $updateList = {} )
#foreach( $entry in $ctx.args.updateMovieInput.keySet() )
  #set( $discard = $updateList.put($entry, "'$ctx.args.updateMovieInput[$entry]'") )
#end
#set( $update = $updateList.toString().replace("{","").replace("}","") )
{
  "version": "2018-05-29",
  "statements":   ["UPDATE Movie SET $update WHERE id='$ctx.args.updateMovieInput.id'", "SELECT * FROM Movie WHERE id='$ctx.args.updateMovieInput.id'"]
}